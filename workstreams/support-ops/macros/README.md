# Macro index

One line per macro file, newest first. Format: `YYYY-MM-DD` | feature | MLS | file | status (Draft, In Intercom, Retired). The `/intercom-macros` skill reads this before drafting so it updates an existing macro instead of duplicating it.

(no macros yet)
