# Build log

## Spec

Bootstrap this repository. It is the shared Claude Code workspace for the Perchwell Customer Success team: non-engineers (Tara, Kelly, Rafe, Jeff, and Leo) working in the Claude Code desktop app, with Notion and Intercom provided by claude.ai connectors rather than any file in the repo. Model it on the marketing repo at /Users/leojedynak/Documents/perchwell-marketing, keeping only what a CS team needs.

Ground rules for the whole build. Before doing anything else, save this entire prompt to docs/build-log.md under a heading called Spec, and keep a checklist under it that you tick off as each phase completes, with every judgment call noted in one line. Work autonomously through every phase and pause only at the two places marked CHECKPOINT. Never write to Intercom. Never commit a file from a confidential folder or a file containing a member name or email. Never use em dashes in anything you write. If Notion or Intercom tools are missing from this session, stop immediately and tell me.

Phase 1, conventions and tools. Read the marketing repo's root CLAUDE.md, README.md, .gitignore, and docs/product-marketing-context.md. List every Notion and Intercom tool available in this session with exact names, grouped into reads and writes, and save the list to docs/connector-tools.md with a note at the top that these come from claude.ai connectors and that settings.json references them.

Phase 2, structure and config. Create .claude/settings.json with a permissions allow list of every Notion and Intercom read tool plus the Notion page create and update tools, using the exact names from docs/connector-tools.md, with the Intercom article create and update tools on the ask list so they always prompt, and Bash allow entries for git status, pull, add, commit, and push. Do not create an .mcp.json. Create a .gitignore covering .claude/settings.local.json, .claude/agent-memory/, the pattern */confidential/ with .gitkeep exceptions, .DS_Store, .env, and credentials. Create these folders with .gitkeep files where empty: .claude/skills/, .claude/commands/, docs/standards/ with content-standards.md, fin-labeling.md, and qa-checklist.md as placeholders, docs/help-center/baldwin/, docs/help-center/crmls/, docs/help-center/changelog.md with a one-line header, workstreams/help-center-overhaul/ with CLAUDE.md, docs/, audit/, golden-questions/, qa/, fin-reports/, outputs/, and confidential/, and workstreams/support-ops/ with CLAUDE.md and macros/.

Phase 3, the documents. Write six files.

Root CLAUDE.md, under 250 lines, using the marketing repo's root CLAUDE.md as the structural model with a CS support voice in place of the marketing brand voice. Sections: what this repo is and who uses it, stating that Notion and Intercom come from claude.ai connectors so a missing tool means the teammate has not connected it in claude.ai settings. A Perchwell quick reference condensed from the marketing product context, keeping only what a support writer needs. Support voice in two registers: help center articles are matter-of-fact and instructional, second person, one action per step, screenshot placeholders in brackets; conversation replies and macros are friendly and competent for a member who may be brand new and non-technical; never em dashes; translate system terms into what the member sees with a "which means" clause; hedge predictions about member behavior. Operational doc conventions ported from marketing: roles and contacts at the top, scenario labels A, B, C for branching answers, audience-named blocks like "Internal note" and "Email snippet," hyperlinked emails. The three-system contract: the repo holds standards, automations, and the Intercom article mirror; Notion is the collaborative review layer; Intercom is production and where Fin reads from. Write rules as a ladder: Notion draft pages are created and updated freely; new Intercom articles are pushed only after Notion approval and always in unpublished draft state, with publishing a human action in Intercom; updates to live Intercom articles require a full before-and-after diff, explicit confirmation per article with no blanket approvals, a changelog entry in docs/help-center/changelog.md, and a mirror re-sync; Claude never writes conversations, contacts, companies, or macros in Intercom; until the team turns pushes on, target September 11, 2026, the repo is read-only toward Intercom. Privacy rule: member names, emails, phone numbers, and addresses never appear in committed files; raw excerpts go in confidential folders only. Git rule: Claude offers to pull at session start and commit and push at session end; teammates never run git. Knowledge management rules ported from marketing: ask before writing to team docs, keep docs current not cumulative, separate instructions in CLAUDE.md files from facts in docs folders, never read outputs folders unless asked. Repository structure with a short note per folder.

README.md for a teammate who has never used a terminal, ten minutes end to end: install the Claude Code desktop app and sign in with the Perchwell Google account; get repo access from Leo and clone with the desktop app's clone flow using the HTTPS URL, describing the actual clicks; open the folder and approve the trust prompt; connect Notion and Intercom in claude.ai under Settings, then Connectors, once each, noting that this works in both claude.ai and Claude Code; then type /setup-check and describe what a passing result looks like. Add short sections on what you can ask Claude to do here with five example requests tied to the help center project, the workstreams and what lives in each, the confidential folder rule, and how syncing works in plain language. Close with troubleshooting for the three most likely failures: Claude says it has no Notion or Intercom tools, a connector asks to be reconnected, and Claude asks for a permission you do not understand. No jargon without a plain-language gloss.

docs/product-context.md, condensed from the marketing product context into a support-writing reference: what Perchwell is, the customer MLSs with Baldwin and CRMLS in scope for this project, user roles, feature names with one line each, the glossary, and terminology rules. Drop personas, objections, and competitive positioning.

workstreams/help-center-overhaul/CLAUDE.md with the project facts: MLS Help Center Standardization and Scalability, September 1 to October 2, 2026, lead Tara, scope Baldwin and CRMLS, target of 10 percent or less human intervention on Fin with Baldwin around 40 percent today, Notion project page ID 3c88b9e0143880ec8fbee25ec0b38975 as the system of record, what lives in each subfolder, the milestone dates of September 4 for standards and Fin labeling, September 11 for audit and backlog, September 18 for articles, September 25 for visuals, and October 2 for QA and Fin validation, the Perchwell definition of a Fin-resolved conversation as Fin answered with no teammate message afterward and no same-topic member follow-up within 48 hours, and a note that the workstream can be archived after October 2.

workstreams/support-ops/CLAUDE.md: evergreen support content that outlives the project, macro drafts in macros/ one file per feature indexed in macros/README.md, Intercom macros never written by Claude.

workstreams/help-center-overhaul/docs/project-status.md and decisions.md with headers and the known contacts: Tara as project lead, Kelly Miragliotta, Rafe Petkovic, Jeff Wakeland, and Leo Jedynak as marketing and repo owner, with titles and emails only if confirmed from Notion and bracketed placeholders otherwise.

Phase 4, ported skills. Port six skills from the marketing repo into .claude/skills/, each with a one-line command file in .claude/commands/, reading each source fully first. intercom-macros from .claude/skills/intercom-macros/, saving to workstreams/support-ops/macros/ and cross-linking against the local mirror with Intercom search as fallback. article-draft adapted from .claude/skills/help-center-article/, reading docs/standards/ and the mirror instead of the marketing snapshot, keeping Notion draft page creation as the review handoff, saving drafts to workstreams/help-center-overhaul/outputs/drafts/, with a clearly marked unimplemented stub for the future Intercom push. article-rewrite from .agents/skills/help-center/ with its nine patterns and references. humanizer, skill-creator, and learn-and-update-repo, with file references updated to this layout. Strip marketing-only references (Google Docs, Slides, Jira, brand voice, blogs) and any path that does not exist here. Then list every path each skill reads or writes and confirm each resolves.

Phase 5, the setup-check skill. Build a skill called setup-check with a /setup-check command that a teammate runs at the start of every session. It pulls the latest from main, confirms the Notion and Intercom tools are present and names the fix in plain language if not, lists the five most recent Baldwin articles from the local mirror as a smoke test, and ends with a one-paragraph plain-language status. The README points every teammate at it.

Phase 6, the help center mirror. Build a skill called sync-help-center with a /sync-help-center command that keeps docs/help-center/baldwin/ and docs/help-center/crmls/ as a faithful mirror of the live Intercom help centers: one markdown file per article named from the slug, with frontmatter carrying the Intercom article ID, title, URL, collection, state, author, created and updated timestamps, and any labels Fin uses, and the body converted to clean markdown; a README.md index per MLS grouped by collection; a sync-state.json recording the last run and per-article timestamps so re-runs fetch only changes and interrupted runs resume; small batched reads with backoff on rate limits; and a per-MLS summary of added, updated, removed, and unchanged on every run. Read-only toward Intercom. Before building, inspect a few articles and a few conversations in Intercom to learn two things: how Baldwin and CRMLS articles are distinguished, and how Baldwin conversations are identified for Phase 7. CHECKPOINT 1: report both findings to me and wait for my confirmation. Then build the skill, run the full sync for both MLSs, run it a second time to confirm zero changes, and record the article counts in the build log. The plan expects about 115 Baldwin articles; if the count is far off, say so in the log.

Phase 7, golden-questions v1. Build a skill called golden-questions with a /golden-questions command that mines real member questions from Baldwin cutover conversations in Intercom. For each conversation capture the member's question in their own words, the topic, whether Fin answered, whether a teammate stepped in, and the article Fin cited. Cluster similar questions into one golden question each, recording canonical phrasing, three real paraphrases, volume, the correct answer in two or three sentences, the target Baldwin article from the mirror, the CRMLS equivalent if one exists, and how Fin handled it. Strip names, emails, phone numbers, addresses, and MLS IDs before writing anything outside workstreams/help-center-overhaul/confidential/, where raw excerpts go. Committed output goes to workstreams/help-center-overhaul/golden-questions/golden-question-set.md. Support --sample N, --since, and --until flags. Read-only toward Intercom. Run it with --sample 25 over the two weeks after the Baldwin cutover date, which you can find in the marketing repo's workstreams/baldwin/docs/project-status.md or ask me for. Grep the committed file for email addresses and names and fix anything that survived. Note any filter problems in decisions.md for Session 2.

Phase 8, verify and push. Check all of these and fix any failure: no .mcp.json exists; every tool named in settings.json appears in docs/connector-tools.md; every command file points at a skill that exists; git status shows nothing from a confidential folder; a grep for @perchwell.com and @gmail.com hits only the contacts sections in project docs; a second /sync-help-center run reports zero changes; /setup-check passes. CHECKPOINT 2: show me the repository tree, the build log, and git status, and wait for my yes. Then commit with the message "Scaffold CS repo: config, skills, help center mirrors, golden-questions v1" and push to main.

## Checklist

- [x] Phase 1, conventions and tools (2026-09-04)
- [x] Phase 2, structure and config (2026-09-04)
- [x] Phase 3, the documents (2026-09-04)
- [x] Phase 4, ported skills (2026-09-04)
- [x] Phase 5, setup-check skill (2026-09-04)
- [x] Phase 6, help center mirror (CHECKPOINT 1 inside) (2026-09-04)
- [x] Phase 7, golden-questions v1 (2026-09-04)
- [x] Phase 8, verify (2026-09-04); push pending CHECKPOINT 2

## Judgment calls

- Session start: Notion and Intercom tools confirmed present as deferred tools before any work began.
- Phase 1: the Intercom connector also exposes add_internal_note and submit_mcp_feedback; both put on a deny list since the spec says Claude never writes conversations and feedback leaves the workspace.
- Phase 1: tool names include a per-connector server ID; assumed identical for every teammate on the Perchwell claude.ai org, flagged in docs/connector-tools.md for verification on the next teammate's first /setup-check.
- Phase 1: Notion session and agent tools (spawn, message, stop) treated as writes and left off the allow list.
- Phase 2: .gitignore uses **/confidential/* rather than the literal */confidential/ because a single * does not cross directory levels and would miss workstreams/<name>/confidential/; the /* form is also what lets the .gitkeep exception work.
- Phase 2: Bash allow entries use the Bash(git status:*) prefix form plus bare forms so both "git pull" and "git pull origin main" pass without a prompt.
- Phase 2: docs/standards placeholders name their owner and target date from the Notion project page so they read as real stubs, not empty files.
- Phase 3: all five contact emails confirmed from the Notion user directory; titles left as bracketed placeholders because the only Notion page listing CS titles is an archived WIP draft.
- Phase 3: Baldwin cutover date (August 3, 2026) taken from the marketing repo's workstreams/baldwin/docs/project-status.md, so the golden-questions window is August 3 to 17.
- Phase 3: README's clone step leads with "ask Claude to clone it in a no-folder session" because that is the flow that worked for Leo today; the app's own Clone button is given as the alternative since its exact labels vary by version.
- Phase 3: CLAUDE.md keeps the generic support@perchwell.com mailbox in the email-hyperlink example; it is a team address, not a person, and the Phase 8 email grep treats it as allowed.
- Phase 3: the "Perchie" name for Fin appears in Notion; flagged [confirm] in product-context.md rather than asserted.
- Phase 4: help-center-article became article-draft with Notion tool names rewritten to the connector-prefixed form and the Intercom push isolated in references/intercom-push.md, which is a stub with a NOT IMPLEMENTED banner and preconditions.
- Phase 4: humanizer's WARP.md dropped (Warp-terminal specific); README rewritten to two paragraphs since the GitHub install steps do not apply inside a repo.
- Phase 4: skill-creator copied wholesale minus __pycache__; it contains no repo-specific paths.
- Phase 4: learn-and-update-repo's four layers changed from brand/skills/agents/workstreams to voice/standards/skills/workstreams because this repo has no .claude/agents/ and docs/standards/ has named approvers.
- Phase 4: article-rewrite saves to outputs/drafts/ with the mirror frontmatter on top and never edits the mirror file, keeping the mirror a faithful copy of Intercom.
- Phase 4: path check found five unresolved references, all expected: the two mirror README.md files and sync-state.json are written by /sync-help-center in Phase 6, the macros index was created empty now, and two were example filenames in worked examples (one reworded).
- Phase 5: setup-check bundles a shell script (scripts/recent-articles.sh) for the mirror smoke test so the listing is deterministic and does not depend on Claude parsing frontmatter by eye.
- Phase 6 inspection: Intercom workspace holds 458 articles (212 published, 246 draft). The connector exposes no help-center or collection listing, so the MLS of an article is derived from its parent collections: a collection is Baldwin or CRMLS if any article in it carries a /baldwin/ or /crmls/ public URL, otherwise default; an article's URL shows the default help center whenever it is also in a default collection, so URL alone undercounts (77 Baldwin by URL, 107 by collection membership against the plan's 115).
- Phase 6 inspection: one collection (16866865) holds articles with both Baldwin and CRMLS URLs; treated as ambiguous pending CHECKPOINT 1.
- Phase 6 inspection: Fin cites articles by content_id (the data-entity-id in its inline citations and ai_agent.content_sources), not by article id; the mirror frontmatter will carry both so golden-questions can map citations back.
- Phase 7 inspection: Baldwin conversations carry custom attribute MLS = Baldwin (set by the "(Members) Baldwin | Support Bot Workflow NEW" workflow, also visible in ai_agent.source_title). The connector cannot filter on that attribute server-side, so the skill filters by date range and ai_agent_participated server-side and by MLS client-side from search_conversations results, which include custom_attributes.
- Phase 6 (after CHECKPOINT 1): help center membership now comes from docs/help-center/collections.md, built by probing the public site (200 = owner, 401 = not owner) for all 68 collection IDs seen in any article's parent_ids; URL inference dropped as Leo directed. Nine collections returned 401 from every help center and are listed with owner "none (401 everywhere)"; their articles are mirrored only if another of their collections is owned.
- Phase 6 counts: Baldwin mirror 123 articles (120 published, 3 draft). Cross-check: search_articles help_center_id 4755399 reports 120 published and 3 drafts. Match.
- Phase 6 counts: CRMLS mirror 85 articles (71 published, 14 draft). Cross-check: search_articles help_center_id 4767477 reports 71 published and 16 drafts. Published matches; two CRMLS drafts are not reachable through any owned collection (their parent_ids point only at collections the probe could not attribute) and are not mirrored. Logged for Session 2.
- Phase 6 counts: default help center (279383) reports 98 published; not mirrored, out of scope.
- Phase 6: the plan's 115 Baldwin articles is 5 short of the 120 live today; noted in audit/notes.md. Not far off.
- Phase 6: 26 articles sit in both Baldwin and CRMLS through shared collections and are mirrored into both folders, as Leo directed.
- Phase 6: article bodies for published articles are fetched from the public help center page rather than get_article, because the connector returns bodies only into the conversation and the first full run would have meant retranscribing about 190 bodies by hand; the public page renders the same HTML (verified on Dotloop Integration: identical text once Intercom's Related Articles block is stripped). Drafts, which have no public page, come from get_article. Frontmatter records body_source per article.
- Phase 6: Intercom image URLs carry expiring signatures; the mirror strips the query string so re-syncs do not churn. Horizontal rules in source articles are dropped in markdown; headings carry the separation.
- Phase 6: article tags are recorded as labels (Fin's labels); only 3 of 458 articles carry any today.
- Phase 6: article 13923637 "Creating a Search with Filters in Perchwell" is CRMLS-only with a Baldwin description; logged in workstreams/help-center-overhaul/audit/notes.md.
- Phase 6: second sync run against the same inventory reported zero added, updated, or removed for both MLSs.
- Phase 6: the mirror contains six generic mailboxes from public article text (support@ and no-reply@perchwell.com, and Baldwin MLS staff mailboxes). No member names or personal emails. The Phase 8 email grep treats these as allowed because they are published help center content, not member data.
- Phase 7: Baldwin conversations pulled with the compact `search` tool filtered by the Baldwin workflow title and a created_at range (637 in August 3 to 17), then reduced to 585 by dropping teammate test traffic. Sampled 25, read each with get_conversation, stored trimmed threads (every part with a body, plus assignment, close, note, tag events, Intercom's resolution_state, and the custom attributes) under confidential/golden-questions/2026-08-03-to-2026-08-17/.
- Phase 7: 20 of the 25 threads arrived inline and were transcribed into the trimmed shape by hand; the 5 largest were saved by the harness and ingested by the script. Fin answer HTML kept its citation anchors so content IDs map back to the mirror; image tags and Source footers were dropped from the transcriptions.
- Phase 7: 19 golden questions written from the 25 conversations; most have only one or two real phrasings in a sample this size, and the file says so per question rather than inventing paraphrases. Two bug and data reports are listed separately as not answerable by an article.
- Phase 7: the identifier check flagged the English word "Will" as a member first name; the check now skips first names that are ordinary words. Committed file passes with no emails, phones, or names from the run.
- Phase 7: Perchwell resolution in the sample is 13 of 25, Intercom's states are 12 assumed, 5 confirmed, 8 escalated; both recorded in the set, never blended. Filter problems logged in workstreams/help-center-overhaul/docs/decisions.md for Session 2.
- Phase 8: all seven checks pass. No .mcp.json; all 42 connector tools in settings.json appear in docs/connector-tools.md; all nine command files resolve to a skill; git would add only .gitkeep files under confidential/; the second sync run reported zero added, updated, or removed for both MLSs; the setup-check smoke test lists five Baldwin articles.
- Phase 8: /setup-check's git pull step fails today with "couldn't find remote ref main" because the GitHub repository has no commits yet; it passes as soon as the first push lands. Not a repo defect.
- Phase 8: the @perchwell.com grep hits, outside the contacts table in project-status.md, are all generic or illustrative: the support@ example in CLAUDE.md, the support footer in the article-draft references, the support@ and no-reply@ mailboxes inside mirrored public articles, and two rule descriptions in this log and decisions.md. No personal addresses outside the contacts table.
