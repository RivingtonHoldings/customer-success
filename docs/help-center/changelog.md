# Help center changelog

One entry per change to a live Intercom article, newest first. Format: date, MLS, article title, what changed, who confirmed.

- 2026-09-23, Baldwin and the default help center, Dashboard Overview (13903062). One sentence: the Listings Widget Overview link line now reads "covers choosing whose listings you see" instead of "covers the scope options". Scope is the Scope by menu's label and nothing on screen calls the control "the scope options", so the article had adopted a control's label as its own noun away from the point of use. Body otherwise byte-identical; title and description unchanged. Confirmed by Tara. Notes: the same sentence links to Listings Widget Overview (13903171), which is retitled to "Find Listings in the Listings Widget" in an unported Notion draft, so the link text will need updating when that article ships. The seven `/en/` cross-links were left alone: the stored HTML has always carried them and the public Baldwin page rewrites them to `/baldwin/en/` on render, which is why the mirror shows the MLS path. `parent_ids` unchanged at 17797865 and 10863226, so this also reaches the NYC and brokerage audience. `author_id` stayed 5472692, which is the acting admin already. No scratch draft needed; the eight existing images were passed back by their CDN paths with the expiring query stripped, and Intercom re-signed them.
- 2026-09-22, Baldwin and the default help center, Dashboard Overview (13903062). Rewrote the Listings widget section, which now states the widget's behavior per scope instead of in one undifferentiated paragraph. **This corrects a live error:** the published text told members to click the pencil icon to edit their listing without saying the widget has to be set to My Listings, so a member reading it in any other scope was looking for a control that is not there. The pencil icon now names its destination, Add/Edit. Added the ShowingTime icon that sits beside the pencil under My Listings and what it opens, and the tag icon that replaces both under MLS Listings, Brokerage Listings, and Office Listings. Replaced the Listings screenshot and added a second one for Office Listings, both with alt text written from the images. Moved the Note callout below the Listings Widget Overview link. Added one sentence to the integrations section pointing at the Listings widget, phrased as a pointer rather than a second copy of the procedure. Title and description unchanged. Confirmed by Tara. Note: `parent_ids` came back from the update as 17797865 and 10863226, confirming this also reaches the NYC and brokerage audience. `author_id` was already 5472692 and did not move this time. SentriLock was left in the integrations list at Tara's direction; it is currently off the Baldwin panel and expected back within 48 hours. Scratch draft 17078914 was used to re-host the two new screenshots and needs deleting by hand in Intercom.
- 2026-09-18, Baldwin and the default help center, Dashboard Overview (13903062). Replaced the pre-standard body with the approved Notion rewrite: added a Loom walkthrough and seven screenshots, all carrying alt text, cut "customizable tiles" and seven uses of "allows you to", removed all bold and eight horizontal rules, repointed seven cross-links from internal intercom.help URLs to public support.perchwell.com URLs, added the Manage widgets section and an Integrations steps block, and narrowed the named integrations from six to three with a line covering the rest. Description rewritten. Title case corrected. Confirmed by Tara. Note: the article sits in a Baldwin collection (17797865) and a default help center collection (10863226), so it also reaches the NYC and brokerage audience, which is outside this project's scope and not mirrored. Screenshots were re-hosted onto Intercom's CDN by passing the Notion URLs to the Articles API, which downloads and re-hosts them. Intercom reassigned author_id from 9490228 to 5472692 on save.
- 2026-09-17, Baldwin and CRMLS, Universal Search Bar (11002620). Swapped the walkthrough video for the re-recorded Loom (d83f9879). Body otherwise byte-identical to the earlier entry today; title and description unchanged. Confirmed by Tara.
- 2026-09-17, Baldwin and CRMLS, Universal Search Bar (11002620). Replaced the body with the approved Notion rewrite: added a When to use section and a mobile note, cut the duplicated Power Search tip, renamed MLS number to MLS ID, removed bold from the body, gave both screenshots alt text, and added cross-links to Create a Search with Filters, Search FAQ, Listing Statuses, and New Terminology. Description rewritten. Title unchanged. Confirmed by Tara. Note: this one record serves both help centers, so CRMLS readers now see Baldwin cross-links; the split is logged in project-status.md. Intercom reassigned author_id from 9490228 to 5472692 on save.
